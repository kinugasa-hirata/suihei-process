{
  "name": "suihei-process",
  "version": "1.0.0",
  "description": "An application to process TXT files to JSON and store in Postgres",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.x.x",
    "dotenv": "^16.x.x",
    "ejs": "^3.x.x",
    "express": "^4.x.x",
    "express-session": "^1.x.x",
    "geoip-lite": "^1.4.10",
    "multer": "^1.x.x",
    "pg": "^8.x.x"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.x"
  }
}
