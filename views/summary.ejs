<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script defer src="/_vercel/insights/script.js"></script>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <title>水平ノズル全数検査成績書</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://unpkg.com/jspdf-font@1.0.7/dist/japanese.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      .container {
        max-width: 1200px;
        margin: 0 !important;
        padding: 0 !important;
      }
      
      .summary-table {
        margin-top: 0.5rem !important;
      }
      
      .nav-links {
        margin-bottom: 1rem !important;
      }
      
      .no-data {
        padding: 1rem !important;
        text-align: center;
        background-color: #f8f9fa;
        border-radius: 5px;
        margin-bottom: 1rem !important;
      }
      
      .highlight-cell {
        background-color: #fff3cd;
        font-weight: bold;
      }
      
      .table-responsive {
        overflow-x: auto;
        padding-top: 5px !important;
        margin: 0 !important;
      }
      
      .file-filter-form {
        background-color: #e9ecef;
        padding: 0.5rem !important;
        border-radius: 5px;
        margin-bottom: 0.5rem !important;
      }
      
      .form-range-group {
        display: flex;
        gap: 0.5rem !important;
        align-items: end;
      }
      
      .range-input {
        width: 120px;
      }

      .content-section {
        background-color: white;
        padding: 0.2rem !important;
        margin: 0 !important;
        max-height: 175px !important;
      }

      .header-info {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 0 !important;
        padding: 0 !important;
        height: auto !important;
        max-height: 50px !important;
      }

      .header-info:nth-child(3) {
        position: relative;
        top: -20px;
        margin-bottom: -20px !important;
      }

      .company-info {
        font-weight: bold;
        font-size: 1.2em;
        position: relative;
        top: -20px;
      }

      .editable {
        border-bottom: 1px solid #ccc;
        padding: 0 2px !important;
        min-width: 50px;
        display: inline-block;
      }

      .date-input, #deliveryDatePicker, input#deliveryDatePicker, flatpickr-input {
        border: none !important;
        background: transparent !important;
        -webkit-appearance: none !important;
        appearance: none !important;
      }
      
      .note {
        margin: 5px 0 !important;
        font-size: 0.9em;
        color: #666;
        padding-left: 2px !important;
      }

      .note::before {
        content: "※";
        margin-right: 2px !important;
      }

      .unit-note {
        text-align: right;
        font-weight: bold;
        margin: 5px 0 10px 0 !important;
        padding: 0 !important;
        clear: both;
        position: relative;
        line-height: 1.2 !important;
      }

      .environment-note {
        margin: 5px 0 10px 0 !important;
        padding: 0 !important;
        line-height: 1.2 !important;
        clear: both;
      }

      .table {
        background-color: white;
        border-collapse: collapse;
        margin: 5px 0 0 0 !important;
        border: 1px solid #dee2e6;
      }

      .table td, .table th {
        text-align: center;
        vertical-align: middle;
        border: 1px solid #dee2e6;
        padding: 2px !important;
      }

      .table thead td {
        background-color: white;
        font-weight: normal;
        text-align: center;
        vertical-align: middle;
        border: 1px solid #dee2e6;
      }

      .table thead tr:last-child {
        display: table-row;
      }

      .table tbody td {
        text-align: center;
        border: 1px solid #dee2e6;
      }

      .filename-cell {
        text-align: center;
        font-weight: bold;
      }

      .smaller-text {
        font-size: 0.9em;
      }

      .button-container {
        position: fixed;
        right: 20px;
        top: 50%;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 10px;
        z-index: 1000;
      }

      .button-container button,
      .button-container a {
        width: 100px;
        padding: 10px;
        text-align: center;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        cursor: pointer;
        font-weight: bold;
      }

      .button-home {
        background-color: #007bff;
        color: white;
        border: none;
        text-decoration: none;
        display: block;
      }

      .button-confirm {
        background-color: #ffc107;
        color: white;
        border: none;
      }

      .button-print {
        background-color: #16862f;
        color: white;
        border: none;
      }

      .button-print:disabled,
      .button-export-pdf:disabled {
        background-color: #cccccc;
        cursor: not-allowed;
        opacity: 0.5;
      }

      #pdf-container {
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        background-color: white;
        overflow: visible;
        padding: 0 5px !important;
      }

      h2.text-center {
        margin: 0 0 2px 0 !important;
        padding: 0 !important;
        line-height: 1.2 !important;
      }

      .invalid-value {
        color: #dc3545 !important;
        font-weight: bold;
        background-color: rgba(220, 53, 69, 0.1);
        position: relative;
      }

      .invalid-value:hover::after {
        content: attr(title);
        position: absolute;
        bottom: 100%;
        left: 50%;
        transform: translateX(-50%);
        padding: 5px;
        background: rgba(0, 0, 0, 0.8);
        color: white;
        border-radius: 4px;
        font-size: 12px;
        white-space: nowrap;
        z-index: 1000;
      }

      .highlight-cell.invalid-value {
        background-color: rgba(220, 53, 69, 0.1) !important;
      }

      .url-display, .timestamp {
        display: block;
      }
      
      .notes-container {
        margin-bottom: 10px !important;
        padding-bottom: 5px !important;
        position: relative;
      }

      .table thead tr:last-child, 
      .column-labels-row,
      tr.print-hide {
        display: none !important;
      }
      
      @media print {
        @page {
          size: A4 landscape;
          margin: 5mm;
        }

        body {
          width: 297mm;
          height: 210mm;
          margin: 0;
          padding: 0;
          -webkit-print-color-adjust: exact;
          print-color-adjust: exact;
        }

        .no-print, .button-container, .url-display, .timestamp {
          display: none !important;
        }

        .editable {
          border: none !important;
          border-bottom: none !important;
        }
        
        .date-input, #deliveryDatePicker {
          border: none !important;
          background: transparent !important;
          -webkit-appearance: none !important;
          appearance: none !important;
        }

        .container {
          width: 100%;
          max-width: none;
          margin: 0;
          padding: 0 !important;
        }

        .table {
          font-size: 9pt;
          margin: 0;
          border-collapse: collapse;
          width: 100%;
          page-break-inside: auto;
          border: 1px solid #000 !important;
        }

        .table td,
        .table th {
          padding: 1px 2px !important;
          border: 1px solid #000 !important;
        }

        .table thead tr:last-child {
          display: none !important;
        }

        .table tr {
          page-break-inside: avoid;
          page-break-after: auto;
          border: 1px solid #000 !important;
          border-top: 1px solid #000 !important;
        }

        h2 {
          font-size: 14pt;
          margin: 0 0 2mm 0 !important;
        }

        * {
          -webkit-print-color-adjust: exact !important;
          print-color-adjust: exact !important;
        }

        .content-section {
          padding: 0 !important;
          margin: 0 !important;
          background-color: white;
          box-shadow: none;
          max-height: 130px !important;
        }
        
        table, th, td, tr {
          border: 1px solid #000 !important;
        }
        
        table {
          border-top: 1px solid #000 !important;
          border-collapse: collapse !important;
        }
        
        tbody tr:first-child {
          border-top: 1px solid #000 !important;
        }
        
        .environment-note {
          margin: 3px 0 5px 0 !important;
          padding: 0 !important;
          line-height: 1.1 !important;
        }
        
        .unit-note {
          margin: 0 0 3px 0 !important; 
          padding: 0 !important;
        }

        .table-responsive {
          padding-top: 5px !important;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div id="pdf-container">
        <div class="content-section">
          <div class="container">
            <h2 class="text-center">水平ノズル全数検査成績書</h2>
            
            <div class="header-info">
              <div>
                <strong>品名:</strong> 水平ノズル
                <strong>仕様書番号:</strong> KDMP-0434
                <strong>担当者:</strong>
                <span class="editable" contenteditable="true" id="inspector"><%= displayName %></span>
              </div>
            </div>
            <div style="height: 1px;"></div>
            <div class="header-info">
              <div style="white-space: nowrap">
                <strong>図番:</strong> P2MM302000 
                <strong>数量:</strong><span id="fileCount"><%= files ? files.length : 0 %></span>個
                <strong>検査方法:</strong> 全数検査 
                <strong>検査日:</strong> <%= new Date().toLocaleDateString('ja-JP', { year: 'numeric', month: '2-digit', day: '2-digit' }).replace(/\//g, '/') %>
                <strong>納期:</strong>
                <input type="text" id="deliveryDatePicker" class="date-input flatpickr" placeholder="YYYY/MM/DD" />
              </div>
              <div class="company-info">㈱平田商店</div>
            </div>
            <div class="header-info">
              <div style="margin-left: auto">
                <button
                  id="quickApproveButton"
                  style="
                    width: 48px;
                    height: 48px;
                    border-radius: 50%;
                    background-color: white;
                    color: white;
                    border: none;
                    font-weight: bold;
                    cursor: pointer;
                    box-shadow: 0 2px 4px white;
                  "
                  title="一括承認"
                ></button>
              </div>
            </div>

            <div class="notes-container">
              <div class="environment-note">
                当社検査における環境は室温20℃±5℃、湿度40%±25%で管理された部屋で実施しております。
              </div>

              <div class="unit-note">
                単位（ｍｍ）
              </div>
            </div>
          </div>
        </div>
        <div style="height: 40px;"></div>

        <div class="container">
          <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger"><%= error %></div>
          <% } %> 
          
          <% if (typeof files !== 'undefined' && files && files.length > 0) { %>
          <div class="table-responsive">
            <table class="table table-bordered table-hover">
              <thead>
                <tr>
                  <td>図面寸法</td>
                  <td>8.2</td>
                  <td>37.5</td>
                  <td>15.9</td>
                  <td>24.1</td>
                  <td>Φ11.2</td>
                  <td>Φ3.2全貫通</td>
                  <td>Φ8.0</td>
                  <td>深さ5.0</td>
                  <td>P.C.D 30</td>
                  <td>Φ155.4</td>
                  <td>83.1</td>
                  <td>Φ122.3</td>
                  <td>嵌め合い</td>
                  <td>目視点検</td>
                  <td>（参考値）</td>
                </tr>
                <tr>
                  <td>寸法公差</td>
                  <td>±0.2</td>
                  <td>±0.3</td>
                  <td>±0.2</td>
                  <td>±0.2</td>
                  <td>＋0.2/0</td>
                  <td>±0.1</td>
                  <td>±0.2</td>
                  <td>±0.1</td>
                  <td>±0.2</td>
                  <td>±0.5</td>
                  <td>±0.3</td>
                  <td>±0.5</td>
                  <td>-</td>
                  <td>-</td>
                  <td>-</td>
                </tr>
                <tr>
                  <td>上限値</td>
                  <td>8.4</td>
                  <td>37.8</td>
                  <td>16.1</td>
                  <td>24.3</td>
                  <td>11.4</td>
                  <td>3.3</td>
                  <td>8.2</td>
                  <td>5.1</td>
                  <td>30.2</td>
                  <td>155.9</td>
                  <td>83.4</td>
                  <td>122.8</td>
                  <td>-</td>
                  <td>-</td>
                  <td>-</td>
                </tr>
                <tr>
                  <td>下限値</td>
                  <td>8.0</td>
                  <td>37.2</td>
                  <td>15.7</td>
                  <td>23.9</td>
                  <td>11.2</td>
                  <td>3.1</td>
                  <td>7.8</td>
                  <td>4.9</td>
                  <td>29.8</td>
                  <td>154.9</td>
                  <td>82.8</td>
                  <td>121.8</td>
                  <td>-</td>
                  <td>-</td>
                  <td>-</td>
                </tr>
                <tr>
                  <td>測定器具</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>ピンゲージ</td>
                  <td>ピンゲージ</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>三次元測定器</td>
                  <td>治具</td>
                  <td>目視</td>
                  <td>秤</td>
                </tr>
                <tr>
                  <td>記載事項</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>合否</td>
                  <td>合否</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>測定値</td>
                  <td>合否</td>
                  <td>合否</td>
                  <td>重量(g)</td>
                </tr>
                <tr class="column-labels-row print-hide">
                  <th>Filename</th>
                  <% var displayHeaders = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J', 'K', 'L', 'M', 'N']; %>
                  <% displayHeaders.forEach(function(header) { %>
                  <th><%= header %></th>
                  <% }); %>
                  <th>質量 (g)</th>
                </tr>
              </thead>
              <tbody>
                <% files.forEach(function(file) { 
                  const displayFilename = file.filename.replace(/\.txt$/i, '');
                %>
                <tr>
                  <td class="filename-cell" data-file-id="<%= file.$id %>">
                    <strong><%= displayFilename %></strong>
                  </td>
                  
                  <td class="<%= fileData[file.$id]['A'].value !== '-' && !fileData[file.$id]['A'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['A'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['B'].value !== '-' && !fileData[file.$id]['B'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['B'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['C'].value !== '-' && !fileData[file.$id]['C'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['C'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['D'].value !== '-' && !fileData[file.$id]['D'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['D'].value %>
                  </td>

                  <td class="highlight-cell data-cell">
                    -
                  </td>

                  <td class="highlight-cell data-cell">
                    -
                  </td>

                  <td class="<%= fileData[file.$id]['G'].value !== '-' && !fileData[file.$id]['G'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell g-value-cell"
                      title="G1: <%= fileData[file.$id]['G1'].value %>&#13;G2: <%= fileData[file.$id]['G2'].value %>&#13;G3: <%= fileData[file.$id]['G3'].value %>&#13;G4: <%= fileData[file.$id]['G4'].value %>">
                    <%= fileData[file.$id]['G'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['H'].value !== '-' && !fileData[file.$id]['H'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['H'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['I'].value !== '-' && !fileData[file.$id]['I'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['I'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['J'].value !== '-' && !fileData[file.$id]['J'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['J'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['K'].value !== '-' && !fileData[file.$id]['K'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['K'].value %>
                  </td>

                  <td class="<%= fileData[file.$id]['L'].value !== '-' && !fileData[file.$id]['L'].isValid ? 'invalid-value' : '' %> highlight-cell data-cell">
                    <%= fileData[file.$id]['L'].value %>
                  </td>

                  <td class="highlight-cell data-cell">
                    -
                  </td>

                  <td class="highlight-cell data-cell">
                    -
                  </td>

                  <td>
                    <% if (file.weight) { %>
                      <%= (parseFloat(file.weight)).toFixed(1) %>
                    <% } else { %>
                      -
                    <% } %>
                  </td>
                </tr>
                <% }); %>
              </tbody>
            </table>
            <div class="bottom-note" style="margin-top: 10px; text-align: left">
              ＊着脱容易なシールに識別番号を記載し製品裏の底面に貼り付ける。
            </div>
          </div>
          <% } else { %>
          <div class="no-data">
            <h3>No Data Available</h3>
            <p class="text-muted">No files match your current filter criteria.</p>
            <a href="/" class="btn btn-primary">Go to Upload Page</a>
          </div>
          <% } %>
        </div>
      </div>

      <!-- Buttons with new "確認" button -->
      <div class="button-container no-print">
        <a href="/" class="button-home">ホーム</a>
        <button onclick="showValidation()" class="button-confirm">確認</button>
        <button id="printBtn" onclick="printReport()" class="button-print" disabled>印刷</button>
        <button id="pdfBtn" onclick="exportPDF()" class="button-export-pdf" style="background-color: #8a2be2; color: white; border: none;" disabled>PDF</button>
      </div>

      <!-- Validation Dialog Modal -->
      <div id="validationModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.7); z-index: 9999; align-items: center; justify-content: center;">
        <div style="background: white; padding: 30px; border-radius: 10px; max-width: 600px; width: 90%; max-height: 80vh; overflow-y: auto;">
          <h3 style="margin-bottom: 20px; color: #333;">📋 印刷前チェック</h3>
          
          <div id="validationResults" style="margin-bottom: 20px;">
            <!-- Results will be inserted here -->
          </div>
          
          <div style="display: flex; gap: 10px; justify-content: flex-end; margin-top: 20px; padding-top: 20px; border-top: 1px solid #ddd;">
            <button onclick="closeValidationModal()" class="btn btn-secondary">閉じる</button>
            <button id="enablePrintBtn" onclick="enablePrintPdf()" class="btn btn-success" style="display: none;">印刷/PDF を有効化</button>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/ja.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://unpkg.com/jspdf-font@1.0.7/dist/japanese.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Initialize Flatpickr
        flatpickr("#deliveryDatePicker", {
          dateFormat: "Y/m/d",
          locale: "ja",
          allowInput: true,
        });

        // Quick approve button logic
        document.getElementById("quickApproveButton").addEventListener("click", function () {
          const rows = document.querySelectorAll("tbody tr");
          rows.forEach((row) => {
            const cells = row.querySelectorAll("td:nth-child(6), td:nth-child(7), td:nth-child(14), td:nth-child(15)");
            cells.forEach((cell) => {
              if (cell.textContent.trim() === "-" || cell.textContent.trim() === "") {
                cell.classList.remove("invalid-value");
                cell.textContent = "合";
              }
            });
          });
        });
      });

      // Validation function - triggered by "確認" button
      function showValidation() {
        const issues = [];
        const warnings = [];
        
        const rows = document.querySelectorAll('tbody tr');
        let missingApprovals = [];
        
        rows.forEach((row) => {
          const cells = row.querySelectorAll('td');
          const filename = cells[0].textContent.trim();
          
          const columnE = cells[5]?.textContent.trim();
          const columnF = cells[6]?.textContent.trim();
          const columnM = cells[13]?.textContent.trim();
          const columnN = cells[14]?.textContent.trim();
          
          if (columnE === '-' || columnF === '-' || columnM === '-' || columnN === '-') {
            missingApprovals.push(filename);
          }
        });
        
        if (missingApprovals.length > 0) {
          issues.push({
            type: 'error',
            title: '❌ 合否が未入力',
            message: `${missingApprovals.length}件のファイルで合否(E,F,M,N)が未入力です。秘密ボタンを押してください。`,
            files: missingApprovals.slice(0, 5).join(', ') + (missingApprovals.length > 5 ? '...' : '')
          });
        }
        
        const inspector = document.getElementById('inspector')?.textContent.trim();
        if (!inspector || inspector === '') {
          issues.push({
            type: 'error',
            title: '❌ 担当者名が未入力',
            message: '担当者名を入力してください。'
          });
        }
        
        const deliveryDate = document.getElementById('deliveryDatePicker')?.value;
        if (!deliveryDate || deliveryDate === '' || deliveryDate === 'YYYY/MM/DD') {
          issues.push({
            type: 'error',
            title: '❌ 納期が未入力',
            message: '納期をカレンダーから選択してください。'
          });
        }
        
        let missingWeights = [];
        rows.forEach((row) => {
          const cells = row.querySelectorAll('td');
          const filename = cells[0].textContent.trim();
          const weight = cells[15]?.textContent.trim();
          
          if (!weight || weight === '-') {
            missingWeights.push(filename);
          }
        });
        
        if (missingWeights.length > 0) {
          issues.push({
            type: 'error',
            title: '❌ 重量が未入力',
            message: `${missingWeights.length}件のファイルで重量が未設定です。`,
            files: missingWeights.slice(0, 5).join(', ') + (missingWeights.length > 5 ? '...' : '')
          });
        }
        
        const lotGroups = {};
        <% files.forEach(function(file) { %>
          const lot_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %> = '<%= file.lot || "未設定" %>';
          const filename_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %> = '<%= file.filename.replace(/\.txt$/i, "") %>';
          
          if (!lotGroups[lot_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %>]) {
            lotGroups[lot_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %>] = [];
          }
          lotGroups[lot_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %>].push(filename_<%= file.$id.replace(/[^a-zA-Z0-9]/g, '_') %>);
        <% }); %>
        
        const lotSheets = [];
        Object.keys(lotGroups).forEach(lot => {
          const files = lotGroups[lot];
          if (files.length > 0 && lot !== '未設定') {
            files.sort((a, b) => {
              const numA = parseInt(a.match(/^\d+/)?.[0] || 0);
              const numB = parseInt(b.match(/^\d+/)?.[0] || 0);
              return numA - numB;
            });
            
            const firstFile = files[0];
            const lastFile = files[files.length - 1];
            
            lotSheets.push({
              lot: lot,
              count: files.length,
              representative: firstFile,
              range: firstFile + (files.length > 1 ? ` 〜 ${lastFile}` : '')
            });
          }
        });
        
        if (lotSheets.length > 0) {
          warnings.push({
            type: 'warning',
            title: '📎 必要なLOT検査成績書',
            message: '以下のLOT検査成績書を準備してください:',
            sheets: lotSheets
          });
        }
        
        displayValidationResults(issues, warnings);
      }

      function displayValidationResults(issues, warnings) {
        const resultsDiv = document.getElementById('validationResults');
        const enableBtn = document.getElementById('enablePrintBtn');
        
        let html = '';
        
        if (issues.length > 0) {
          html += '<div style="margin-bottom: 20px;">';
          issues.forEach(issue => {
            html += `
              <div style="background: #fff3cd; border-left: 4px solid #ffc107; padding: 12px; margin-bottom: 10px; border-radius: 4px;">
                <div style="font-weight: bold; margin-bottom: 5px;">${issue.title}</div>
                <div style="font-size: 14px;">${issue.message}</div>
                ${issue.files ? `<div style="font-size: 12px; color: #666; margin-top: 5px;">対象: ${issue.files}</div>` : ''}
              </div>
            `;
          });
          html += '</div>';
          enableBtn.style.display = 'none';
        } else {
          html += '<div style="background: #d4edda; border-left: 4px solid #28a745; padding: 12px; margin-bottom: 20px; border-radius: 4px;">';
          html += '<div style="font-weight: bold; color: #155724;">✓ すべての必須項目が入力されています</div>';
          html += '</div>';
          enableBtn.style.display = 'inline-block';
        }
        
        if (warnings.length > 0) {
          warnings.forEach(warning => {
            html += '<div style="background: #e3f2fd; border-left: 4px solid #0d6efd; padding: 12px; margin-bottom: 10px; border-radius: 4px;">';
            html += `<div style="font-weight: bold; margin-bottom: 10px;">${warning.title}</div>`;
            html += `<div style="font-size: 14px; margin-bottom: 10px;">${warning.message}</div>`;
            
            if (warning.sheets) {
              html += '<ul style="margin: 0; padding-left: 20px;">';
              warning.sheets.forEach(sheet => {
                html += `
                  <li style="margin-bottom: 8px;">
                    <strong>ロット: ${sheet.lot}</strong><br>
                    <span style="font-size: 13px; color: #666;">
                      範囲: ${sheet.range} (${sheet.count}件)<br>
                      添付: <strong>${sheet.representative}</strong> のLOT検査成績書
                    </span>
                  </li>
                `;
              });
              html += '</ul>';
            }
            
            html += '</div>';
          });
        }
        
        resultsDiv.innerHTML = html;
        document.getElementById('validationModal').style.display = 'flex';
      }

      function closeValidationModal() {
        document.getElementById('validationModal').style.display = 'none';
      }

      function enablePrintPdf() {
        document.getElementById('printBtn').disabled = false;
        document.getElementById('pdfBtn').disabled = false;
        closeValidationModal();
        alert('✓ 印刷とPDFボタンが有効になりました！');
      }

      function printReport() {
        const urlElements = document.querySelectorAll('.url-display, .timestamp');
        urlElements.forEach(el => {
          el.classList.add('no-print');
        });

        const headerRow = document.querySelector('.table thead tr:last-child');
        if (headerRow) {
          headerRow.classList.add('no-print');
          headerRow.style.display = 'none';
        }
        
        const tables = document.querySelectorAll('table');
        tables.forEach(table => {
          table.style.borderCollapse = 'collapse';
          table.style.border = '1px solid #000';
          
          const firstRow = table.querySelector('tbody tr:first-child');
          if (firstRow) {
            firstRow.style.borderTop = '1px solid #000';
          }
        });

        const deliveryDateInput = document.getElementById('deliveryDatePicker');
        if (deliveryDateInput) {
          deliveryDateInput.style.backgroundColor = 'transparent';
        }

        setTimeout(() => {
          window.print();
        }, 200);
      }

      function exportPDF() {
        const loadingEl = document.createElement("div");
        loadingEl.style.position = "fixed";
        loadingEl.style.top = "0";
        loadingEl.style.left = "0";
        loadingEl.style.width = "100%";
        loadingEl.style.height = "100%";
        loadingEl.style.backgroundColor = "rgba(255,255,255,0.8)";
        loadingEl.style.display = "flex";
        loadingEl.style.justifyContent = "center";
        loadingEl.style.alignItems = "center";
        loadingEl.style.zIndex = "9999";
        loadingEl.innerHTML = '<div style="background-color: #fff; padding: 20px; border-radius: 5px; box-shadow: 0 0 10px rgba(0,0,0,0.2);">PDFを生成中です。お待ちください...</div>';
        document.body.appendChild(loadingEl);

        try {
          prepareForPDF();
          
          const pdf = new jspdf.jsPDF({
            orientation: 'landscape',
            unit: 'mm',
            format: 'a4'
          });
          
          addJapaneseFontSupport(pdf);
          
          const element = document.getElementById('pdf-container');
          
          html2canvas(element, {
            scale: 2,
            useCORS: true,
            logging: false,
            allowTaint: true,
            backgroundColor: '#ffffff'
          }).then(canvas => {
            const imgData = canvas.toDataURL('image/png');
            
            const imgWidth = 287;
            const imgHeight = canvas.height * imgWidth / canvas.width;
            
            pdf.addImage(imgData, 'PNG', 5, 5, imgWidth, imgHeight);
            
            const today = new Date();
            const dateStr = `${today.getFullYear()}${String(today.getMonth() + 1).padStart(2, '0')}${String(today.getDate()).padStart(2, '0')}`;
            pdf.save(`水平ノズル検査成績書_${dateStr}.pdf`);
            
            restoreAfterPDF();
            
            document.body.removeChild(loadingEl);
          }).catch(error => {
            console.error("Error generating canvas:", error);
            alert("PDF生成中にエラーが発生しました: " + error.message);
            restoreAfterPDF();
            document.body.removeChild(loadingEl);
          });
        } catch (error) {
          console.error("Error generating PDF:", error);
          alert("PDF生成中にエラーが発生しました。ブラウザのコンソールで詳細を確認してください。");
          restoreAfterPDF();
          document.body.removeChild(loadingEl);
        }
      }

      function prepareForPDF() {
        document.querySelectorAll('.no-print').forEach(el => {
          el.style.display = 'none';
        });
        
        const headerRow = document.querySelector('.table thead tr:last-child');
        if (headerRow) {
          headerRow.style.display = 'none';
        }
        
        document.querySelectorAll('table, th, td').forEach(el => {
          el.style.border = '1px solid #000';
        });
        
        const container = document.getElementById('pdf-container');
        if (container) {
          container.style.backgroundColor = 'white';
        }
        
        const inspectorElement = document.getElementById('inspector');
        if (inspectorElement) {
          inspectorElement.style.border = 'none';
          inspectorElement.style.borderBottom = 'none';
        }
        
        const deliveryDateInput = document.getElementById('deliveryDatePicker');
        if (deliveryDateInput) {
          deliveryDateInput.style.border = 'none';
          deliveryDateInput.style.backgroundColor = 'transparent';
        }
      }

      function restoreAfterPDF() {
        document.querySelectorAll('.no-print').forEach(el => {
          el.style.display = '';
        });
        
        const headerRow = document.querySelector('.table thead tr:last-child');
        if (headerRow) {
          headerRow.style.display = '';
        }
        
        const inspectorElement = document.getElementById('inspector');
        if (inspectorElement) {
          inspectorElement.style.border = '';
          inspectorElement.style.borderBottom = '';
        }
        
        const deliveryDateInput = document.getElementById('deliveryDatePicker');
        if (deliveryDateInput) {
          deliveryDateInput.style.border = '';
          deliveryDateInput.style.backgroundColor = '';
        }
      }

      function addJapaneseFontSupport(pdf) {
        try {
          if (typeof japaneseFont !== 'undefined') {
            japaneseFont.addFontToDocument(pdf);
          }
        } catch (error) {
          console.warn("Japanese font support not available:", error);
        }
      }
    </script>
  </body>
</html>